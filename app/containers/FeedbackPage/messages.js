/*
 * FeedbackPage Messages
 *
 * This contains all the text for the FeedbackPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FeedbackPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FeedbackPage container!',
  },
});
